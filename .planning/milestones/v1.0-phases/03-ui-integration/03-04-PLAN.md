---
phase: 03-ui-integration
plan: 04
type: execute
wave: 3
depends_on: ["03-01", "03-03"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "All UI components render correctly and the build succeeds"
    - "Template management UI is visible on Settings page"
    - "Posting workflow is functional on LateDaysTracking page"
  artifacts: []
  key_links: []
---

<objective>
Visual verification that all Phase 3 UI components render correctly across Settings and LateDaysTracking pages.

Purpose: Confirm the full posting workflow is visually correct and functionally wired before marking the phase complete.
Output: User confirmation that all UI elements are present and functional.
</objective>

<execution_context>
@/Users/mapajr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mapajr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-ui-integration/03-01-SUMMARY.md
@.planning/phases/03-ui-integration/03-02-SUMMARY.md
@.planning/phases/03-ui-integration/03-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify Phase 3 UI Integration</name>
  <what-built>
Complete UI integration for late day comment posting:
1. Settings page: Template management card with two textareas (penalty/non-penalty), variable reference, save button
2. LateDaysTracking page: Posting panel with assignment dropdown, student selection, preview modal, confirmation dialog, progress indicator, posting history table, already-posted badges, production warning, override comment, dry run mode
  </what-built>
  <how-to-verify>
1. Start the development servers:
   - Backend: `uv run uvicorn main:app --host 0.0.0.0 --port 8000 --reload` (from project root)
   - Frontend: `cd canvas-react && npm run dev` (port 5173)
2. Visit http://localhost:5173/settings
   - Verify "Comment Templates" card appears below Sync History
   - Verify two textarea fields (penalty and non-penalty) are populated with default templates
   - Verify variable reference box shows 5 variables
   - Edit a template and click "Save Templates" — verify success message appears
3. Visit http://localhost:5173/late-days
   - Verify "Post Comments" button appears in the header
   - Click "Post Comments" to expand the posting panel
   - Verify assignment dropdown, student checkboxes, override textarea, dry run checkbox, and Preview button
   - Select an assignment — verify students with late days are auto-selected
   - Click "Preview Comments" — verify preview modal opens with rendered comments
   - In preview modal, verify "Post Comments" button opens confirmation dialog
   - Verify confirmation dialog shows course name, assignment name, student count, and mode
4. If on production course, verify yellow warning banner appears (SAFE-04)
5. Run build to confirm no errors: `cd canvas-react && npm run build`
  </how-to-verify>
  <resume-signal>Type "approved" if all UI elements are correct, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
User confirms all UI components render correctly.
</verification>

<success_criteria>
User approves the visual verification checkpoint.
</success_criteria>

<output>
After completion, create `.planning/phases/03-ui-integration/03-04-SUMMARY.md`
</output>
